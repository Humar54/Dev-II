using System.Collections.Generic;
using UnityEngine;

public class InputManager : MonoBehaviour
{
    //[InputAxis]
    //[SerializeField] private string _fireInput;

    //[Dropdown("intValues")]
    //public int intValue;

    [SerializeField] private List<Ressource> _ressourceCost;


    // Start is called before the first frame update
    void Start()
    {

    }

    // Update is called once per frame
    void Update()
    {
        //if (Input.GetButton(_fireInput))
        //{
        //  print("Fire");
        // }
    }
}
